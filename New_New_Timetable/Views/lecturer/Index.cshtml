@model IEnumerable<New_New_Timetable.lecturer>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.lecturer_name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.lecturer_name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.lecturer_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.lecturer_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.lecturer_id })
        </td>
    </tr>
}

</table>
